# Core API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic-settings==2.7.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
pgvector==0.3.6

# Pipeline (Phase 1: ingest.py only)
pymupdf==1.25.1
FlagEmbedding==1.3.4

# torch is a FlagEmbedding dependency. The line below installs CPU-only torch.
# On this machine (RTX 4060 Ti, 16GB VRAM) install the CUDA build instead, before
# installing the rest of this file:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch==2.5.1

# Pipeline (Phase 2: retrieve.py, llm.py, generate.py, graph.py, eval/runner.py)
openai==2.54.0
langgraph==1.2.11
numpy==2.5.3
ragas==0.4.3
langchain-core==1.6.2

# Pipeline (Phase 3: style_check.py)
# Pinned to the version already installed ad hoc per README.md when
# measure_style.py produced style_guide.md section 2's numbers — a
# different textstat version can score Flesch-Kincaid differently, which
# would silently invalidate the measured 3.0-4.5 target.
textstat==0.7.13

# Pipeline (Phase 4: verify.py)
# Already installed transitively via FlagEmbedding; pinned explicitly here
# because verify.py now imports it directly.
sentence-transformers==6.0.1

# Phase 5: auth
# passlib 1.7.4 is unmaintained and touches the stdlib crypt module (removed
# in Python 3.13) via its bcrypt backend probe — fine on this project's
# Python 3.12. We only use its argon2 backend (argon2-cffi does the real
# work); pinned explicitly rather than left transitive, same treatment as
# sentence-transformers above.
pyjwt==2.10.1
passlib[argon2]==1.7.4
argon2-cffi==23.1.0
email-validator==2.2.0

# Tests
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
